/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Resolve @ alias to src (shared with existing components)
  transpilePackages: [],
};

export default nextConfig;
